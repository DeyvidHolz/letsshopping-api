import { Messages } from '../types/messages/message.types';

const shopInfoMessages: Messages = {
  created: 'Shop Info created.',
  updated: 'Shop Info updated.',
};

export default shopInfoMessages;
